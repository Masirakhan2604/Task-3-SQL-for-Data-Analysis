# Task-3-SQL-for-Data-Analysis

Objective

Use SQL queries to extract insights and perform analysis using an Ecommerce dataset.

Tools Used

SQLite (lightweight and easy to use)

VS Code (with SQLTools + SQLite extensions)

Excel (for initial data cleaning)

SQLite CLI (for importing the CSV)
